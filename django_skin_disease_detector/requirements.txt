# Django Framework
Django>=5.2.0
djangorestframework>=3.14.0
django-cors-headers>=4.0.0

# AI/ML - Versiones actualizadas y compatibles
tensorflow>=2.20.0
keras>=3.12.0
numpy>=2.2.0,<2.3.0
pillow>=12.0.0
scikit-learn>=1.7.0
pandas>=2.3.0
opencv-python>=4.12.0
h5py>=3.15.0

# Para cálculos estadísticos avanzados
scipy>=1.11.0   # Biblioteca científica para estadísticas, distancias y cálculos (ej: Mahalanobis).

# Para gestión de bases de datos
dj-database-url>=1.0.0  # Permite entender cadenas DATABASE_URL y simplifica la configuración de la BD.
django-environ  # Facilita la gestión de variables de entorno y configuración basada en entorno.

# Base de datos y almacenamiento de archivos estáticos
django-storages>=1.13.1 # Soporte para almacenamiento en la nube (Azure, S3, etc.)
azure-storage-blob>=12.20.0 # Cliente para Azure Blob Storage
whitenoise>=6.5.0 # Sirve archivos estáticos de manera eficiente en producción.

# Parametros de entorno
python-decouple # Gestión de configuración y variables de entorno
python-dotenv # Carga variables de entorno desde archivos .env

# HTTP requests
requests # Biblioteca para hacer solicitudes HTTP de manera sencilla.

# Azure (CLI not required as package, but optional Key Vault integration)
azure-identity>=1.12.0 # Manejo de identidades y autenticación en Azure
azure-keyvault-secrets>=4.10.0 # Acceso a secretos almacenados en Azure Key Vault

# PDF generation
xhtml2pdf # Biblioteca para convertir HTML/CSS a PDF
reportlab # Biblioteca para generar PDFs dinámicamente
